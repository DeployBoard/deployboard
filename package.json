{
  "name": "deployboard",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "24.x",
    "npm": "11.x"
  },
  "scripts": {
    "build": "turbo run build",
    "build:schema": "turbo run build:schema",
    "start": "turbo run start",
    "dev": "turbo run dev --parallel",
    "mongo": "docker run -itd --rm -p 27017:27017 -v db-data:/data/db --name mongo mongo:8",
    "docs": "docker run -itd --rm -p 8000:8000 -v ${PWD}/apps/docs:/docs/ --name docs squidfunk/mkdocs-material:latest",
    "lint": "turbo run lint",
    "test": "playwright test --retries=0",
    "test:debug": "PWDEBUG=1 playwright test",
    "format": "prettier --write \"**/*.{js,jsx,md}\"",
    "seed": "turbo run seed",
    "clean": "turbo run clean --parallel && rm -rf node_modules && rm -rf package-lock.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "dotenv": "^17.2.3",
    "playwright": "^1.57.0",
    "prettier": "latest",
    "turbo": "^2.6.3"
  },
  "volta": {
    "node": "24.12.0",
    "npm": "11.6.2"
  }
}
